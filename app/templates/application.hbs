<div class="container">
  {{#paper-toolbar}}
    {{#paper-toolbar-tools}}
      {{#paper-menu as |menu|}}
        {{#menu.trigger}}
          {{#paper-button iconButton=true}}
            {{paper-icon "menu"}}
          {{/paper-button}}
        {{/menu.trigger}}
        {{#menu.content width=4 class="main-app-menu" as |content|}}
          {{#content.menu-item onClick="openDialog"}}
            {{paper-icon "help"}}
            <span>Instructions for using this app</span>

          {{/content.menu-item}}
          {{#each menuitems as |item|}}
            {{#content.menu-item onClick=(action "externalLink" item)}}
              {{paper-icon item.icon}}
                <span>{{item.title}}</span>
            {{/content.menu-item}}
          {{/each}}
        {{/menu.content}}
      {{/paper-menu}}
      <h2>
        {{#link-to 'index'}}FunSet Enrichment Visualization{{/link-to}}
      </h2>
      <span class="flex"></span>
      {{#each navigation.dynamicbuttons as |dynamicbutton|}}
        {{component dynamicbutton}}
      {{/each}}
    {{/paper-toolbar-tools}}
  {{/paper-toolbar}}
  {{#if showDialog}}
    {{#paper-dialog class="flex-77" onClose=(action "closeDialog" "cancel") origin=dialogOrigin clickOutsideToClose=true}}
      {{#paper-toolbar}}
        {{#paper-toolbar-tools}}
          <h2>Instructions for use (help menu)</h2>
          <span class="flex"></span>
          {{#paper-button iconButton=true onClick=(action "closeDialog" "cancel")}}{{paper-icon icon="close"}}{{/paper-button}}
        {{/paper-toolbar-tools}}
      {{/paper-toolbar}}

      {{#paper-dialog-content}}
        {{usage-instructions}}
      {{/paper-dialog-content}}
    {{/paper-dialog}}
  {{/if}}
  {{paper-toaster}}
  <div class="layout-row flex">
    <div class="flex">{{outlet}}</div>
  </div>
  <div class="footer layout-row layout-align-space-between-end">
    <div class="left-footer">
      <p class="credit">
        © 2017 <a href="http://faculty.ist.unomaha.edu/mhale/" target="_blank" style="color: inherit;"> Matthew L. Hale</a>, <a href="http://faculty.ist.unomaha.edu/dghersi/" target="_blank" style="color: inherit;">Dario Ghersi</a>, &amp; Ishwor Thapa
      </p>
    </div>
    <div class="center-footer">
    </div>
    <div class="right-footer">
      <iframe src="https://ghbtns.com/github-btn.html?user=mlhale&repo=funset-enrichment-visualization&type=star&count=true&size=large" frameborder="0" scrolling="0" width="115px" height="30px"></iframe>
      <a href="https://twitter.com/mlhale_?ref_src=twsrc%5Etfw" class="twitter-follow-button" data-size="large" data-show-screen-name="false" data-show-count="false">Follow @mlhale_</a><script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>
    </div>
  </div>
</div>
